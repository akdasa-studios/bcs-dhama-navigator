import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.akdasa.bcsdhamanavigator',
  appName: 'BCS Dhama Navigator',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
